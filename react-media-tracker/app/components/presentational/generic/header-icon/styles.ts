import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	icon: {
		margin: 10,
		marginRight: 15,
		alignSelf: 'center',
		width: 30,
		height: 30
	},
	iconDisabled: {
		opacity: 0.5
	}
});
